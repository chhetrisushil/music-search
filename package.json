{
  "name": "music-search",
  "version": "1.0.0",
  "description": "search music using spotify api",
  "main": "index.js",
  "scripts": {
    "start": "http-server -c-1 -p 9000",
    "build": "webpack",
    "build:dev": "webpack -w",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "http-server": "^0.10.0",
    "webpack": "^2.5.0"
  }
}
